# .Rprofile for IntroRPython4DS book project
# Automatically loads reticulate and sets Python virtual environment

library(reticulate)
# Force reticulate to use the virtual environment
Sys.setenv(RETICULATE_PYTHON_FALLBACK = FALSE)
use_virtualenv("IntroRPython4DS", required = TRUE)
